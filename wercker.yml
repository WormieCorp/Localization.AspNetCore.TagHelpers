box: admiringworm/dotnet-mono

build:
  steps:
    - script:
      name: "Restoring all packages"
      code: bash build -r -c Release
    - script:
      name: "Building Projects"
      code: bash build -b -c Release
    - script:
      name: "Running unit tests"
      code: bash build -t -c Release