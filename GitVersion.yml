assembly-versioning-scheme: None
mode: ContinuousDelivery
branches:
  master:
    mode: ContinuousDelivery
    tag: ''
  dev(elop)?(ment)?$:
    tag: alpha
    track-merge-target: true
ignore:
  sha: []

